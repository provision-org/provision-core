{
  "name": "@provision/daemon",
  "version": "0.1.0",
  "description": "Provision workforce agent task orchestrator",
  "type": "module",
  "main": "dist/index.js",
  "bin": {
    "provisiond": "dist/index.js"
  },
  "scripts": {
    "build": "tsc",
    "bundle": "esbuild src/index.ts --bundle --platform=node --target=node22 --format=esm --outfile=bundle/provisiond.mjs",
    "dev": "tsx src/index.ts",
    "test": "vitest run"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "esbuild": "^0.28.0",
    "tsx": "^4.0.0",
    "typescript": "^5.7.0",
    "vitest": "^3.0.0"
  }
}
